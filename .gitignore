.lake/
*.olean
__pycache__/

# gerado por higiene/Higiene.lean — nunca versionado (senao vira sombra e conflita
# entre os repos). Regra 5 do higiene: o mapa e' GERADO, nunca escrito a mao.
MAPA.md

# gerado pela cadeia. ATENCAO: `deposito/` e `bolhas/` NAO entram aqui — no atelie
# essas duas pastas sao o CONTEUDO versionado, e ignora-las ali desligaria a
# versionagem do acervo em silencio. Neste repositorio elas sao so lixo de execucao,
# e por isso a regra vive em .git/info/exclude (local, nao compartilhado).
/prova-*.txt
/recibo-*.txt
