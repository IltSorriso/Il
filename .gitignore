.lake/
*.olean
__pycache__/

# gerado por higiene.py — nunca versionado (senao vira sombra e conflita entre os repos)
MAPA.md

# gerado pela cadeia (arreio.py / reproduzir-cadeia.sh) — mesma regra.
# ANCORADOS na raiz de proposito: sem a barra inicial, `deposito/` engoliria
# tambem `exemplos/deposito/`, que e' o CANONE versionado. Ancorar protege o canon.
/deposito/
/bolhas/
/prova-*.txt
