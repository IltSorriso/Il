/- Corredor: importa o `Arreio` JÁ COMPILADO (o `.olean`) e usa a CLI dele.
   Compilar o `Arreio.lean` inteiro a cada uso custa ~30 s; importar o `.olean`
   fresco custa uma fração disso. O `main` vem do próprio módulo importado. -/
import Arreio
