# Woodpecker CI (Codeberg) — verificação do JUÍZ (Lean 4)
when:
  event: push

steps:
  verificar-juiz:
    image: leanprovercommunity/lean:latest
    commands:
      - lean --version
      - lean juiz/Bolha.lean
